<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carwashapp">

    <!-- 📌 Permisos globales necesarios -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- 📌 Permisos para mapa -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- 📌 Permisos para tomar foto -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- Android 12 o menor -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Car Wash App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

    <!-- 🔹 Pantalla principal (inicio de sesión) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 Pantalla de registro -->
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- 🔹 Pantalla de recuperación de contraseña -->
        <activity android:name=".RecuperarContrasenaActivity" android:exported="false" />

        <!-- 🔹 Pantalla principal de usuario -->
        <activity android:name=".HomeActivity" android:exported="false" />

        <!-- 🔹 Pantalla de perfil del usuario -->
        <activity android:name=".PerfilUsuarioActivity" android:exported="false" />

        <!-- 🔹 Pantalla de agregar vehículo -->
        <activity android:name=".AgregarVehiculoActivity" android:exported="false" />

        <!-- 🔹 Pantalla de calificación de servicio -->
        <activity android:name=".CalificacionServicioActivity" android:exported="false" />

        <!-- 🔹 Pantalla de historial de servicios -->
        <activity android:name=".HistorialServiciosActivity" android:exported="false" />

        <!-- 🔹 Pantalla del mapa -->
        <activity android:name=".MapaUbicacionActivity" android:exported="false" />

        <!-- 🔹 Pantalla de solicitud de cotización -->
        <activity android:name=".SolicitarCotizacionActivity" android:exported="false" />

        <activity android:name=".VehiculoActivity" android:exported="false" />

        <!-- 🔹 Pantalla base -->
        <activity android:name=".MainActivity" android:exported="false" />

        <activity android:name=".CambiarContrasenaActivity" android:exported="false" />

        <activity android:name=".ConfirmarCodigoActivity" />



    </application>

</manifest>
