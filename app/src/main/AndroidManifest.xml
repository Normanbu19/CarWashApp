<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carwashapp">

    <!-- ==============================
         📌 PERMISOS GENERALES
    =============================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ==============================
         📌 PERMISOS GPS / UBICACIÓN
    =============================== -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ==============================
         📌 PERMISOS MULTIMEDIA / CÁMARA
    =============================== -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 13 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Android 12 y menor -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ==============================
         📌 CONFIGURACIÓN GENERAL
    =============================== -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Car Wash App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- ==============================
             📌 GOOGLE MAPS API KEY (AQUÍ!)
        =============================== -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDVKBejrM9Nmlj1EE9cawGQ7x5JNqfwKYQ" />

        <!-- ==============================
             ACTIVIDADES DE LA APP
        =============================== -->

        <!-- Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".RecuperarContrasenaActivity" android:exported="false" />
        <activity android:name=".HomeActivity" android:exported="false" />
        <activity android:name=".PerfilUsuarioActivity" android:exported="false" />
        <activity android:name=".AgregarVehiculoActivity" android:exported="false" />
        <activity android:name=".CalificacionServicioActivity" android:exported="false" />
        <activity android:name=".HistorialServiciosActivity" android:exported="false" />
        <activity android:name=".SolicitarCotizacionActivity" android:exported="false" />
        <activity android:name=".VehiculoActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".CambiarContrasenaActivity" android:exported="false" />
        <activity android:name=".ConfirmarCodigoActivity" android:exported="false" />
        <activity android:name=".AdminCotizacionesActivity" android:exported="false" />
        <activity android:name=".AdminUsuariosActivity" android:exported="false" />
        <activity android:name=".AdminVehiculosActivity" android:exported="false" />
        <activity android:name=".AdminDashboardActivity" android:exported="false" />

        <!-- Mapa Google (ya no OSMDroid) -->
        <activity android:name=".MapaUbicacionActivity" android:exported="false" />

    </application>

</manifest>
